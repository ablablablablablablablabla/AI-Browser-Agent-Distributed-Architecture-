# 🤖 AI Autonomous Browser Agent (Chrome Extension + Python)



**Автономный AI-агент**, работающий как расширение для Google Chrome. Агент управляет реальным браузером пользователя, выполняет многошаговые задачи (поиск работы, шоппинг, работа с почтой) и принимает решения на основе анализа DOM-структуры и визуального контекста страницы.

Проект разделён на **Backend** (Python / FastAPI) и **Frontend** (Chrome Extension), что обеспечивает чистую архитектуру и соответствие требованиям ТЗ.


### 🎥 Демонстрация работы
[![Watch Demo](https://img.shields.io/badge/Смотреть_Демо-Google_Drive-4285F4?style=for-the-badge&logo=google-drive&logoColor=white)](https://drive.google.com/file/d/1YoUDOBHfNAA9MxSLHPRRJ_e1GNxd_evV/view?usp=sharing)

---

## ✨ Ключевые возможности

* **🕵️‍♂️ Полная автономность**
  Агент самостоятельно строит цепочку действий (клики, ввод текста, скролл), работая в клиентском цикле до достижения цели.

* **🍪 Persistent Sessions**
  Агент использует текущий профиль Chrome пользователя — все логины, куки и сессии (Gmail, LinkedIn, HH.ru и др.) сохраняются без повторной авторизации.

* **🧠 Sub-Agent Architecture**
  Динамическое определение типа задачи (`RECRUITER`, `SHOPPER` и др.) с загрузкой специализированных системных промптов и стратегий поведения.

* **🛡️ Security Layer (Human-in-the-loop)**
  Перед выполнением критических действий (оплата, отправка форм, удаление данных) агент обязан запросить явное подтверждение пользователя через UI.

* **👁️ Smart DOM & Vision**
  Кастомный DOM-парсер отфильтровывает только интерактивные элементы для экономии токенов. Дополнительно используется анализ скриншотов для понимания визуального контекста.

---

## 📂 Структура проекта

Проект логически разделён на две части для удобной разработки в соответствующих IDE.

---

### 1. Backend (PyCharm) 🐍

*Логика принятия решений, маршрутизация агентов и работа с LLM.*

```text
backend/
├── main.py              # 🚀 Точка входа. Запускает FastAPI сервер и настраивает CORS.
├── ai_service.py        # 🧠 Мозг системы: обработка DOM, выбор агента, запросы к OpenAI.
├── prompts.py           # 🎭 System Prompts, роли агентов и протоколы безопасности.
├── schemas.py           # 📋 Pydantic-модели данных для фронта и бека.
├── requirements.txt     # 📦 Список зависимостей backend.
└── .env                 # 🔐 (Создаётся вручную) API-ключи.
```

---

### 2. Frontend / Chrome Extension (WebStorm) 🌐

*Управление браузером, DOM и пользовательский интерфейс.*

Frontend оформлен как изолированный модуль с собственным `package.json` — это подчёркивает масштабируемость и готовность к усложнению (сборка, линтинг, тесты).

````text
extension/
├── package.json        # 📦 Метаданные фронтенда, скрипты и контроль зависимостей.
├── manifest.json       # ⚙️ Manifest V3: разрешения, sidePanel.
├── sidepanel.html      # 🖥️ UI боковой панели.
├── sidepanel.js        # 🎮 Клиентский цикл агента и подтверждения пользователя.
├── content.js          # 👁️ Парсинг DOM и выполнение действий (клик, ввод, скролл).
├── background.js       # 👻 Service Worker для событий браузера.
└── icons/              # 🎨 (Опционально) иконки расширения.
```text
extension/
├── manifest.json        # ⚙️ Manifest V3: разрешения, sidePanel.
├── sidepanel.html       # 🖥️ UI боковой панели.
├── sidepanel.js         # 🎮 Клиентский цикл агента и подтверждения пользователя.
├── content.js           # 👁️ Парсинг DOM и выполнение действий (клик, ввод, скролл).
└── background.js        # 👻 Service Worker для событий браузера.
````

---

## 🚀 Установка и запуск

### Шаг 1. Настройка Backend

1. Убедитесь, что установлен **Python 3.10+**.

2. Создайте файл `.env` рядом с `main.py`:

```env
OPENAI_API_KEY=sk-proj-...
```

> Примечание: проект рассчитан на использование `gpt-5-chat-latest` (или `gpt-4o`, в зависимости от доступности).

3. Установите зависимости:

```bash
pip install -r requirements.txt
```

4. Запустите сервер:

```bash
python main.py
```

FastAPI сервер будет доступен по адресу:

```
http://127.0.0.1:8000
```

---

### Шаг 2. Настройка Frontend (Chrome Extension)

1. Откройте Google Chrome и перейдите на:

```
chrome://extensions/
```

2. Включите **Developer mode**.
3. Нажмите **Load unpacked**.
4. Выберите папку `extension/` (файл `manifest.json` должен быть в корне).

---

## 🎮 Использование

1. Откройте сайт, на котором нужно выполнить задачу (например, `hh.ru` или `avito.ru`).
2. Откройте **Side Panel** расширения.
3. Опишите задачу в текстовом поле, например:

* «Найди вакансии Python Junior в Москве и открой первую из них.»
* «Добавь в корзину самый дешёвый чехол для iPhone 13.»

4. Нажмите **🚀 Start Analysis**.

Агент начнёт анализировать страницу, скроллить и выполнять действия.
При необходимости опасного действия процесс будет приостановлен до подтверждения пользователя.

---

## 🏗️ Соответствие требованиям ТЗ

| Требование           | Реализация                                                                        |
| -------------------- | --------------------------------------------------------------------------------- |
| Управление браузером | Нативное API Chrome (`chrome.tabs`, `chrome.scripting`), без Selenium.            |
| Persistent Sessions  | Используется текущий профиль Chrome со всеми куками и логинами.                   |
| Context Management   | Фильтрация DOM в `content.js` + сжатие контекста в `ai_service.py`.               |
| Продвинутые паттерны | Sub-Agents, Error Handling, Security Layer реализованы полностью.                 |
| No Hardcode          | Агент не знает CSS-селекторы заранее — используются динамические `data-agent-id`. |

---

## 🧭 Идея проекта

Это не просто расширение для браузера.

Это **AI-агент**, который:

* видит страницу,
* понимает цель,
* принимает решения,
* и действует в реальной среде пользователя.

Минимум магии. Максимум контроля.
